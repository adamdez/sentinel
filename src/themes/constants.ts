/** localStorage key — keep stable for migrations */
export const SENTINEL_THEME_STORAGE_KEY = "sentinel-theme";

/** Supabase user_profiles.preferences path (optional sync later) */
export const SENTINEL_THEME_PREFS_KEY = "sentinelTheme" as const;

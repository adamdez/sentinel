export const metadata = {
  title: "Lead Intake Queue — Sentinel",
  description: "Review and claim new PPL leads from intake queue",
};

export default function IntakeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
